#include <fstream>

#include "keysystem.hpp"
#include "tools.hpp"

int main(int argc, char* argv[]) {
  // Inicializa variables
  KeySystem system_a;
  // Menu General
  GeneralMenu();

  // Menu Cerradura
  // KeyMenu();

  return 0;
}